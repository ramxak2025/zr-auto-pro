// Re-export all types from shared module.
// This file maintains backwards compatibility — all existing imports
// like `from '../types'` continue to work without changes.
export * from '../../../shared/types';
