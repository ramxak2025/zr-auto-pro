/**
 * ДВУХШАГОВЫЙ ВХОД С ВЫБОРОМ ФИЛИАЛА (163) — общие константы шага 2.
 *
 * Живут отдельным файлом, потому что нужны ОБЕИМ сторонам обмена и импорт
 * auth.service → jwt.strategy был бы циклом: сервис подписывает промежуточный
 * токен, стратегия обязана его отбивать. Разъехавшиеся строки здесь означали
 * бы, что промежуточный токен принимается как обычный, — то есть работу вообще
 * без филиала.
 */

/**
 * Назначение промежуточного токена (claim `purpose`). Токен подписан тем же
 * ключом, что и обычный, поэтому назначение — единственное, что физически
 * отличает «я только ввёл пароль» от «я полноценная сессия». JwtStrategy
 * отбивает такой токен на входе, до любых обращений к базе.
 */
export const POINT_SELECT_PURPOSE = 'point_select';

/**
 * Время жизни промежуточного токена. МИНУТЫ, а не часы: за это окно человек
 * выбирает филиал из списка на экране входа. Длинный TTL превратил бы его в
 * второй пароль — украденный со скриншота токен пускал бы в аккаунт.
 * Пяти минут хватает и на «отвлёкся на звонок», и на медленную сеть.
 */
export const POINT_SELECT_TTL_SECONDS = 300;
