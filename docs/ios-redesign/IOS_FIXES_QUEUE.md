# iOS Fixes Queue — после тестов владельца

Статус: **ГОТОВО к сборке** (2026-05-30). Все 18 пунктов реализованы; Волны 0–3 + аудит выполнены. Проверки зелёные.

## Итог проверок (раздел H + аудит)
- mobile: `typecheck` ✅ · `lint` ✅ · `jest` 176/176 ✅ (plate-mask в т.ч.)
- backend: `typecheck` ✅ · `lint` ✅ · `build` ✅
- frontend: `typecheck` ✅ (общий контракт не сломан)
- Миграции 055–060, идемпотентные/аддитивные; 001–054 не трогали.
- Адверсариальный review диффа (7 измерений) → 16 подтверждённых проблем → **все 16 исправлены** (2 high, 7 medium, 7 low).
- Рантайм-импорты (expo-audio/blur/print/sharing, view-shot, gesture-handler, flash-list) проверены — установлены.
- Нативные конфиги (app.json/plugins/modules) НЕ менялись → prebuild не требуется; сборка из существующего `ios/Autexa.xcworkspace`.
- НЕ проверено в этой среде: реальная установка на физический iPhone (это делает владелец через Xcode Release) — см. acceptance-чеклисты в отчётах агентов.

---

## Решения по открытым вопросам (приняты тим-лидом, можно менять)
- **#5 видимость:** новые булевы поля `hidden_from_schedule`, `hidden_everywhere` у `users` (миграция 055). Фильтрация на клиенте по контексту. Историю чеков НЕ прячем. Управляют director/admin/superadmin.
- **#9 штрафы:** отдельная таблица `salary_penalties` (amount, description, date, created_by). Вычитается из итоговой ЗП. Статус подтверждения уже в данных (`salary_payment_confirmations`) — вывести на карточку.
- **#11 одобрение расходов:** флаг `approval_required` на `expense_categories` + существующий `can_add_expenses`/`daily_expense_limit` у user. Дефолт false (бэкомпат).
- **#14 авто-расход:** при добавлении в подсобку → расход в категории «Имущество» (lookup-or-create). При удалении — диалог «расход остаётся / вернуть деньги в оборот» (реверс). Линк `equipment_item_id`/`storage_item_id` в expenses.
- **#15 клиенты:** история чеков привязана к `car_id` (UUID) — переназначение номера БЕЗОПАСНО. «Без номера» — флаг/пустой plate (миграция). Иностранный номер — фреймворк есть, доделать UI. Последняя оценка — из `review_responses` (endpoint last-rating). Звонки — `recordingUrl` уже есть на бэке, вывести в карточку.
- Хвост миграций = **054**, новые с **055**.

## План по волнам
- **Волна 0 (ios-engineer):** EmptyState без пружины (#3,#12); `ModalBlurBackdrop` + блюр в `Modal.tsx`/`BottomSheet.tsx` (#7 база).
- **Волна 1 (backend-engineer):** миграции 055+ и контракт для #5, #9, #11, #14, #15. Frontend typecheck зелёный.
- **Волна 2 (экранные агенты, параллельно):** #4, #6, #9-UI, #10, #11-UI, #13, #14-UI, #15, #16, #17, #1-полиш. Каждый адаптирует блюр в своих кастомных модалках.
- **Волна 3:** #18 perf/security/audit + Android parity.
- **Волна 4 (qa-build-engineer):** typecheck/lint/jest/plate-mask/prebuild/pod/xcodebuild → открыть Xcode.

---

---

## 19. Клиенты — переделка (правки после теста, 2026-05-31) ⭐
- **Экран:** `ClientsScreen.tsx`, `ClientDetailScreen.tsx`. Скрин IMG_4998.
- **19.1 Отступы:** убрать огромные кривые вертикальные пустоты (фильтр-чипы и «Розничный покупатель» висят в пустоте). Та же болезнь, что легенда расписания — растягивающийся контейнер/flex-спейсер. Прижать, компактно.
- **19.2 Иконки фильтров:** у чипов «Все» и «Новые» до сих пор кружки-заглушки. Перебрать ВСЕ значки/иконки и сделать корректные (не lucide-Circle).
- **19.3 Дизайн раздела (главное):**
  - При входе — **режим поиска по АВТО (госномеру) по умолчанию**, дизайн поиска ru/int **как в Кассе** (`RussianPlateInput`), быстрый.
  - Переключатель режима убрать сегмент-тогглом снизу; вынести **значком вверху справа рядом с «+ добавить клиента»**.
  - Результат поиска по авто — **аккуратные стильные карточки**: госномер + авто + клиент гармонично.
  - Режим «Клиенты» (по значку справа) — поиск именно клиентов по **имени / авто / телефону**, там фильтры и расширенный поиск.
  - **Баг:** при создании клиента **источник не выбирается — список не открывается**. Починить (SourcePickerSheet).
- **19.4 Карточка клиента:**
  - Блок «Только для владельца» → переименовать **«Только для сотрудников»**, видят **все сотрудники** автосервиса (снять owner-gate).
  - **Автомобили**: показать все авто клиента красивыми карточками — что за авто (напр. «Лада Приора»), госномер, **последний пробег**, и **сумма, потраченная клиентом на ЭТО авто** в нашем сервисе.
  - **Убрать** раскрытие чеков по тапу на авто — ниже уже есть «**История чеков**» (продажи + возвраты). Всё с правильными отступами и значками.

---

## 1. Анимация первого открытия приложения (cold start → Login)
- **Экран:** запуск приложения → страница входа (`LoginScreen.tsx`) + прелоадеры.
- **Что нужно:** крутая анимация первого открытия приложения — момент, когда открывается страница входа, и прелоадеры.
- **Ожидание:** премиальный iOS-feel при cold start (splash → плавное появление формы входа, анимированные прелоадеры).
- **Открытые вопросы (уточнить перед стартом):**
  - стиль анимации (fade/scale логотипа, liquid glass reveal, поэтапное появление полей)?
  - брендовый логотип Autexa в анимации?
  - прелоадеры = скелетоны контента после логина или спиннер на самой форме входа?

---

## 2. Склад — модалка «Корректировка»: тонировка → блюр + свайп-закрытие
- **Экран:** Склад (`ProductsScreen.tsx`), нажатие «Корректировка» → bottom sheet снизу.
- **Что не так:**
  1. Окно всплывает снизу с **тёмной тонировкой** (dim overlay). Тёмную тонировку убрать → заменить на **блюр** (liquid glass / `expo-blur`).
  2. Окно нельзя закрыть жестом. Нужно: **потянуть пальцем вниз → окно закрывается** (swipe-to-dismiss / drag handle).
- **Ожидание:** iOS-нативный bottom sheet с блюр-подложкой и pull-to-dismiss.

---

## 3. Склад — убрать прыгающую анимацию «нет товаров»
- **Экран:** Склад (`ProductsScreen.tsx`), состояние пустого списка.
- **Что не так:** когда товара нет — есть прыгающая (bounce) анимация empty-state «нет товаров».
- **Ожидание:** убрать прыгающую анимацию (статичный/спокойный empty-state).

---

## 4. Расписание — огромный отступ вокруг блока легенды статусов
- **Экран:** Расписание (`ScheduleScreen.tsx`), вкладка «График».
- **Что не так:** блок легенды статусов (`✓ Отработано`, `✓ Смена`, `☾ Вых`, `Б/Л`, `<1ч`) имеет **огромный отступ сверху и снизу** — между сегмент-контролом (График/Сегодня/Смены/Рейтинг/Настройки) и легендой, и между легендой и таблицей-сеткой большие пустые промежутки.
- **Ожидание:** убрать лишний вертикальный отступ (paddingTop/paddingBottom/marginVertical) вокруг блока легенды — прижать легенду ближе к табам сверху и к сетке снизу.
- **Подтверждено скриншотом** (Май, IMG_4984).

---

## 5. Настройки → Пользователи — управление видимостью сотрудников
- **Экран:** Настройки → Пользователи (`UsersScreen.tsx`), затрагивает Расписание (`ScheduleScreen`), Рейтинг посещаемости, Сотрудники (`EmployeesScreen`), Касса/чек (`CheckCreateScreen`).
- **Что нужно:** добавить функционал управления отображением сотрудников. Два уровня скрытия:
  1. **Скрыть из Расписания и Рейтинга посещаемости** — сотрудник остаётся в системе, но не показывается в графике и в рейтинге.
  2. **Скрыть полностью из всех разделов** — например, владелец сервиса: убрали везде → его нет ни в расписании, ни в списке сотрудников, на него **нельзя пробить чек** (не выбирается как мастер в заказ-наряде) и т.д.
- **Ожидание:** в карточке/настройках пользователя — переключатели видимости (per-section или общий «скрыть везде»).
- **Зона ответственности (важно):** фича сквозная — **backend** (`users/` модуль + миграция: флаги видимости, фильтрация в выдаче), **shared** контракт (`shared/types`, `createServices`), **mobile** + предположительно **web**. Backend-правки требуют явного согласования (правило D из CLAUDE.md) — при старте подниму как отдельный под-вопрос.
- **Открытые вопросы (уточнить перед стартом):**
  - где хранить флаги — новые поля у `User` (`hiddenFromSchedule`, `hiddenEverywhere`) или отдельная таблица настроек?
  - «скрыть везде» должно прятать и исторические данные (старые чеки на этого мастера) или только запрещать новые?
  - кто может управлять видимостью — только owner/superadmin или ещё director?

---

## 6. Поставщики — галочки, текст кнопки, блюр модалки
- **Экран:** Поставщики (`SuppliersScreen.tsx` / `SupplierDetailScreen.tsx`).
- **Что не так:**
  1. **Галочки отображаются некорректно** — зелёный круг, и внутри него зелёная галочка (сливается, плохой контраст). Поправить.
  2. **Текст кнопки «Покупка б/у товара»** — там не описание кнопки, а тавтология. Переписать описание/подпись кнопки по смыслу.
  3. При добавлении товара снизу открывается окно **«Выберите товар»** — та же проблема, что в Складе: **тёмное затемнение → убрать, добавить плавный блюр** + pull-to-dismiss (см. #2).

---

## 7. ГЛОБАЛЬНО — убрать тёмные затемнения у всех модалок → блюр
- **Где:** везде, где всплывает bottom sheet / модалка с тёмной тонировкой (Склад «Корректировка» #2, Поставщики «Выберите товар» #6, и **все остальные похожие места** в приложении).
- **Что нужно:** убрать dim/тёмное затемнение подложки → заменить на красивый плавный блюр (liquid glass / `expo-blur`). Желательно единый общий компонент-обёртка для всех bottom sheet'ов.
- **Ожидание:** единый iOS-нативный стиль модалок: блюр-подложка + (где уместно) свайп-закрытие.

---

## 8. Поставщики — дата при добавлении поставки (накладные задним числом)
- **Экран:** Поставщики, добавление поставки/накладной.
- **Что нужно:** при добавлении поставки дать возможность **указать дату**, чтобы можно было заводить накладные за прошлые дни (задним числом).
- **Зона ответственности:** вероятно затронет **backend** (поле даты у поставки/прихода + миграция) и **shared**-контракт. Поднять как под-вопрос при старте.

---

## 9. Зарплата — анимации, статус подтверждения на карточке, штрафы
- **Экран:** Зарплата (`SalaryScreen.tsx`), backend `salary/`.
- **Что нужно:**
  1. **Крутые анимации** (те, что просил ранее): **конфетти** при получении зарплаты + анимация «письмо/конверт» при выплате. (В истории уже есть «Salary envelope+confetti» — проверить, работает ли, довести до ума.)
  2. На **самой карточке** сотрудника должно быть видно **не заходя внутрь**, подтвердил ли сотрудник зарплату или нет (статус подтверждения прямо на карточке). И чтобы **функционал реально работал** — зарплата критичная часть приложения.
  3. **Штрафы**: должны быть указаны в зарплате + возможность **наложить штраф с описанием**.
- **Ожидание:** надёжный, «премиальный» раздел зарплаты — видимый статус подтверждения, рабочие штрафы с комментарием, эффектные анимации выплаты/получения.
- **Зона ответственности:** штрафы и статус подтверждения вероятно затронут **backend** (`salary/` + миграция) и **shared**-контракт. Поднять под-вопросом при старте.
- **Открытые вопросы:**
  - штраф — отдельная сущность с суммой+описанием+датой, вычитается из итоговой ЗП?
  - «подтвердил сотрудник» — уже есть в данных или нужно новое поле/endpoint?

---

## 10. Движение денег (бывш. «Касса по дням») — переименование, переключатель периода, баг иконки, детализация дня
- **Экран:** `CashFlowScreen.tsx`.
- **Что нужно:**
  1. **Вернуть название** раздела → «**Движение денег**» (сейчас «Касса по дням»).
  2. Вверху справа 4 кнопки **День / Неделя / Месяц / Год** выглядят неаккуратно — переделать UX/UI в духе Apple (нативный сегмент-контрол / аккуратный селектор периода).
  3. В режиме «по сотруднику»: при выборе сотрудника справа от имени какой-то **жирный кружок** — скорее всего иконка/символ, который **не прогрузился**. Исправить (подставить корректный SF Symbol / убрать артефакт).
  4. При тапе на **день** — аккуратно и красиво открывать **списком чеки**: госномер + сумма. Если был **расход** — на что расход и сколько.
- **Ожидание:** чистый Apple-like экран движения денег с понятной детализацией по дню (доходы-чеки с госномером/суммой + расходы с назначением/суммой).

---

## 11. Расходы — баг иконок (кружки вместо галочек) + контроль прав/одобрения расходов
- **Экран:** Расходы (`ExpensesScreen.tsx`), backend `expenses/`.
- **Что не так / нужно:**
  1. Некоторые значки отображаются **кружком** — например при выборе сотрудника или при выборе категории «по категориям» там должна быть **галочка**. Исправить (тот же класс бага, что #10.3 — не подгружается символ).
  2. У **владельца автосервиса** должна быть возможность **регулировать**: кто может указывать расходы, и какие расходы **требуют одобрения**, а какие нет. Должно быть понятно и удобно по UX/UI.
- **Зона ответственности:** п.2 — сквозная фича: **backend** (`expenses/` права + флаг «требует одобрения» + миграция), **shared**-контракт, mobile (+ возможно web). Поднять под-вопросом при старте.

---

## 12. ГЛОБАЛЬНО — убрать пружинную анимацию empty-state «Ничего не найдено» везде
- **Где:** все экраны с пустым состоянием / «Ничего не найдено» (Склад #3, Расходы, и **везде** по приложению).
- **Что не так:** анимация появления empty-state — пружинка/bounce — выглядит некрасиво.
- **Что нужно:** **убрать пружинку везде** (статичное, спокойное появление empty-state). Желательно единый общий empty-state компонент без bounce.

---

## 13. Отчёты → «Финансовые отчёты» — переименование, обрезка чисел, PDF, цели, прогноз, шеринг-картинка
- **Экран:** Отчёты (`ReportsScreen.tsx`). Подтверждено скриншотом (IMG_4985, Месяц).
- **Что нужно:**
  1. **Переименовать** раздел «Отчёты» → «**Финансовые отчёты**», добавить **подпись/подзаголовок** в духе «Полный контроль над деньгами» (подобрать лучший вариант копирайта самому).
  2. **Обрезается текст/числа** — баг по высоте строки:
     - блок «ЧИСТАЯ ПРИБЫЛЬ» — большое число (`16 150 ₽`) **обрезано сверху и снизу** (line-height/clipping);
     - **маржинальность** — половина обрезана;
     - блок «**Прогноз**» — обрезается верхушка, где указана сумма.
     - Исправить line-height / paddings / `numberOfLines`/overflow у крупных цифр по всему экрану.
  3. **Кнопка «Скачать PDF»**: окошко «Готовим PDF…» нравится, но **не закрывается** даже после того как PDF сформирован — починить (закрывать по завершении/ошибке).
  4. **Дизайн самого PDF** слабый — сделать **красивый и удобный** UX/UI отчёта (современная вёрстка PDF).
  5. Кнопка «**Изменить цели**» открывает окно с **затемнением фона** → убрать затемнение, сделать плавно появляющийся **блюр** (см. #7).
  6. В конце экрана **убрать текст про то, что «что-то не подключено»** — это лишняя информация.
  7. **Шеринг-картинка + PDF**: формирование картинки и PDF должно быть красивым, современным UX/UI. **Картинка — формат Stories** (вертикальная) с основной важной информацией — такая, которую **не стыдно выложить в сторис**.
- **Ожидание:** премиальный финансовый отчёт без обрезанных чисел, рабочий PDF-flow с красивым PDF, блюр-модалки, и эффектная stories-картинка для шеринга.

---

## 14. Имущество — кнопки добавления, истёкший срок службы (красный, не удалять), авто-расход при покупке
- **Экран:** Имущество (`EquipmentScreen.tsx`), backend `equipment/` + `expenses/`.
- **Что нужно:**
  1. На уровне «**подсобка**»: кнопку **добавления папки** сделать красиво — **под последней папкой** (видна только тем, у кого есть доступ).
  2. Внутри папки: кнопку **добавления товара** оставить **круглой** как сейчас, но разместить **внизу справа**, чуть выше островка меню (floating tab bar). Учитывать `useTabBarHeight()`.
  3. Когда **срок службы имущества заканчивается** — элемент просто становится **красноватого оттенка**. **НЕ удалять автоматически**.
  4. При добавлении в подсобку чего-то нового на сумму — **автоматически** создавать **расход** в разделе Расходы, категория «**Имущество**», с аккуратным указанием, **что именно куплено**.
- **Зона ответственности:** п.4 — сквозная: **backend** (`equipment/` создаёт запись в `expenses/`, категория «Имущество») + **shared**-контракт. Поднять под-вопросом при старте.
- **Логика (ответ владельца):** добавил имущество → сервис купил за свои деньги → **это идёт в Расходы** (категория «Имущество»). При **удалении** имущества — **выбор**:
  1. удалил, но **расход понёс** → расход остаётся;
  2. удалил и **вернул деньги в оборот** (возврат) → расход реверсится/отменяется.
  Т.е. при удалении показывать диалог выбора «расход остаётся / вернуть деньги в оборот».
- **Открытые вопросы (частично закрыты выше):**
  - авто-расход создаётся сразу при добавлении имущества (атомарно на бэке) или связка на фронте? (реализация)

---

## 16. Сотрудники — карточки в стиле Имущества + смысл карточки сотрудника + баг кривой модалки
- **Экран:** Сотрудники (`EmployeesScreen.tsx`, `EmployeeDetailScreen.tsx`).
- **Что нужно:**
  1. **Список**: карточки сделать **по дизайну как в разделе Имущество**, но со **своими значками** на карточке.
  2. **Карточка сотрудника** (детально): поправить **дизайн и смысл** карточки. Должна отображать **работу сотрудника**, его **сильные и слабые стороны**, **достижения и награды**. Нужен **анализ сотрудника по всем параметрам**, и карточка открывается **красиво оформленной** под наш стиль.
  3. **Баг:** при **добавлении значка / редактировании** окно открывается **криво — в самом верху, выше экрана**, и приложение **зависает** из-за этого. Нужно: красивое **плавное появление по центру** с **блюром**, закрытие по тапу на **заблюренную область**.
- **Ожидание:** премиальная аналитическая карточка сотрудника + единый стиль карточек-списка как в Имуществе + рабочая центрированная блюр-модалка (см. #7).
- **Открытые вопросы:**
  - «анализ по всем параметрам» — из каких данных (чеки/выработка, посещаемость/смены, рейтинг, ЗП)? какие метрики считаем сильными/слабыми сторонами?
  - «достижения и награды» — есть ли сущность ачивок или генерируем из метрик?

---

## 17. «Ещё» — перегруппировка разделов + новый раздел «База знаний» + убрать «Корзину»
- **Экран:** Ещё (`MoreScreen.tsx`).
- **Что нужно:** разложить разделы по правильным группам:
  - **Работа:** Расписание, **Клиенты**, **База знаний** (⚡ НОВЫЙ раздел — создать, поставить **заглушку**, пока пусто; в будущем: учебный центр, регламенты, база знаний с поиском).
  - **Финансы:** Движение денег, Зарплата, Расходы, Финансовые отчёты.
  - **Склад:** Поставщики, Имущество, Складская аналитика.
  - **Маркетинг:** Отзывы и репутация, Звонки, Рассылки, Интеграции.
  - **Остальное:** все оставшиеся разделы.
- **Отдельно:** **убрать раздел «Корзина»** из «Ещё» — он уже давно на Складе.
- **Ожидание:** чистая группировка «Ещё» по 5 группам (Работа / Финансы / Склад / Маркетинг / Остальное) + работающая заглушка «База знаний».
- **Замечания по согласованности:** имена должны совпасть с переименованиями — «Движение денег» (#10), «Финансовые отчёты» (#13). Проверить наличие экранов «Рассылки» и «Интеграции» (возможно part of `marketing/`).

---

## 18. ГЛОБАЛЬНО — аудит кода, оптимизация, безопасность, баги, максимальная скорость + красивые загрузки
- **Где:** весь репозиторий (mobile приоритет, backend, shared, frontend).
- **Что нужно:**
  1. **Проверить весь код**, оптимизировать где надо.
  2. **Безопасность** — проверить.
  3. **Баги** — найти и исправить.
  4. **Максимальная скорость**: всё открывается **молниеносно** и с **правильной, не устаревшей** информацией (а не stale).
  5. Где требуется подождать — **красивая анимация загрузки**, с которой легче ждать.
  6. **Уклон:** приложение, которое работает **очень быстро и надёжно**.
- **Тонкий момент (зафиксировать):** «быстро + не устаревшее» — это баланс. Сейчас глобально `placeholderData: prev => prev` (stale-while-revalidate) + persistent cache + foreground revalidation (последний коммит `0a741a9`). Нужно: мгновенный показ из кэша, но **обязательная фоновая ревалидация** + индикатор обновления, чтобы данные не были устаревшими.
- **Это финальный сквозной этап** — выполнять после точечных правок #1–#17, отдельным проходом (audit → fix → perf → QA). Кандидат на multi-agent workflow (fan-out по разделам, adversarial verify багов, perf-проход).
- **Зона ответственности:** затрагивает всё. Backend-правки безопасности — согласовать отдельно (правило D).

---

## 15. Клиенты — полный редизайн раздела (дизайн + функционал) ⭐ КРУПНЫЙ
- **Экран:** Клиенты (`ClientsScreen.tsx`, `ClientDetailScreen.tsx`), backend `clients/`, `cars/`, `calls/`, `marketing/`. Подтверждено скриншотом (IMG_4987).
- **Общее:** сейчас бардак и в дизайне, и в функционале. Сделать **красивый современный раздел** под стиль приложения и наши правила дизайна (autexa-visual-system).

### 15.1 Поиск
- Лёгкий поиск авто **по госномеру**, с **акцентом на госномере** — как при создании чека в Кассе (`CheckCreateScreen` плашка/`RussianPlateInput`).
- Возможность **переключиться** и искать клиента **по номеру телефона**.
- Возможность посмотреть **все чеки на розничного покупателя**.
- Поиск **шустрый**, современный подход даже при огромных базах клиентов.
- Поиск **и по русскому, и по иностранному** госномеру.

### 15.2 Карточка клиента (вход через госномер или телефон)
- Красивая карточка клиента, современный UX/UI.
- **Гараж**: видны все его авто.
- Все его **чеки** — целиком или **конкретно по каждому авто**.
- Удобно **отредактировать всё**.
- **Звонки** с этим клиентом — в карточке, можно **прослушать** записи.
- ⭐ **Важно:** **последняя оценка** клиента — насколько он оценил работу сервиса → видна его **лояльность**.
- Возможность добавить **пометку** к клиенту (аккуратно, по желанию).

### 15.3 Редактирование авто + переназначение госномера (data-integrity!)
- Легко сменить **марку/модель** авто или **госномер** **без потери истории чеков**.
- Если **госномер уже есть в базе** → либо **отменить изменения**, либо **подтвердить**; при подтверждении номер **уходит с машины другого клиента**.
- Нужен **фильтр «без номеров»**.

### 15.4 Добавление клиента — источник
- Выбор **источника** должен работать: либо **выбрать из списка**, либо, если источники не настроены, **не добавлять** источник.
- **Список источников** («откуда узнал») **настраивается в разделе Маркетинг**.

### 15.5 Добавление авто
- Можно указать госномер **русский ИЛИ иностранный** (сейчас иностранный **не идёт** — баг).
- Галочка **«без номеров»** → авто добавляется без номеров.

### Зона ответственности / риск
- Сквозная и чувствительная фича: **backend** (`clients`/`cars` — переназначение plate без потери истории чеков; `marketing` — настраиваемый список источников; `calls` — записи в карточке; рейтинг/оценка лояльности), **shared**-контракт, **mobile** (+ вероятно web). Plate-reassignment — зона риска по целостности данных (чеки не должны осиротеть).
- **Открытые вопросы (поднять при старте):**
  - оценка лояльности берётся из `marketing/` отзывов (последний review клиента)? есть ли связь review↔client?
  - записи звонков — реально хранятся/доступны (`calls/`)? есть ли аудио-URL?
  - переназначение plate: история чеков привязана к `car_id` или к строке plate? (определяет, теряется ли история)
  - «без номеров» — отдельный флаг у авто или пустой plate? нужен консистентно с фильтром.
  - иностранный номер — уже есть тип в данных или нужно расширить модель авто/валидацию (`plateMask`)?

---

## 20. Суперадмин ЛК — отдельный дизайн (НЕ как автосервис) ⭐ КРУПНЫЙ (после /compact)
- **Зона:** mobile `AdminScreen.tsx`; web `pages/admin/{AdminDashboardPage,AdminTenantsPage,AdminTenantDetailPage,AdminPlansPage}.tsx`; backend `plans/` + `tenants/`; `FeatureGate.tsx` (mobile+web).
- **Что нужно (слова владельца):**
  1. **ЛК суперадмина — полностью свой UX/UI**, не интерфейс автосервиса. Отдельный «админский» вид.
  2. **Клиенты (тенанты) + их показатели** — список автосервисов-клиентов со стат (выручка/активность/кол-во юзеров/текущий тариф и т.д.), карточка клиента с метриками.
  3. **Управление тарифами** — суперадмин регулирует **стоимость** тарифа и **какие разделы/фичи** в него входят (редактор плана: цена + набор feature-ключей).
  4. **Заглушка для закрытых разделов** — если у пользователя раздел НЕ входит в тариф, при переходе в него — **красивая заглушка** с описанием раздела и указанием, **в каком тарифе он открывается** (улучшить `FeatureGate` locked-state: иконка/описание/название нужного тарифа + CTA «обновить тариф»).
- **Уже есть:** AdminScreen (mobile), 4 admin-страницы (web), `FeatureGate` (mobile+web), backend `plans`/`tenants` модули, `@Roles`/`roles.guard`. Суперадмин обходит гейты (`FeatureGate`: superadmin → children).
- **Backend** (правки plans/tenants) — согласовать при старте (правило D), но владелец уже просит фичу.
- **Открытые вопросы:** какие именно метрики тенанта показывать; список feature-ключей разделов (взять из FEATURE_GATES в AppNavigator + web); тариф = набор feature-ключей + цена + лимиты?
