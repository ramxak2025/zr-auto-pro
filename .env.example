# Database (REQUIRED — docker-compose will fail without these)
DB_PASSWORD=CHANGE_ME_strong_password_here

# JWT (REQUIRED — used for auth token signing)
JWT_SECRET=CHANGE_ME_random_64_char_string

# Frontend API URL (for Docker build)
# Use /api for production (nginx proxies to backend)
VITE_API_URL=/api

# S3 Storage (for product photos)
S3_ENDPOINT=https://s3.timeweb.cloud
S3_REGION=ru-1
S3_BUCKET=zr-auto-pro
S3_ACCESS_KEY=your-access-key
S3_SECRET_KEY=your-secret-key

# Expo / EAS Build (for mobile APK builds)
EXPO_TOKEN=your-expo-token

# Auto-deploy webhook (REQUIRED)
DEPLOY_SECRET=CHANGE_ME_webhook_secret
DEPLOY_BRANCH=master
